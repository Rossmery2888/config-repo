spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/bankapp
      database: customerdb
  application:
    name: customer-service

server:
  port: 8081

logging:
  level:
    org.springframework.data.mongodb.core: DEBUG
    com.example.mscustomerservice: DEBUG