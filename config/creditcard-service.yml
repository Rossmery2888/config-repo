spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/creditcarddb
      database: creditcarddb
  application:
    name: creditcard-service

server:
  port: 8086

logging:
  level:
    org.springframework.data.mongodb.core: DEBUG
    com.example.mscreditcardservice: DEBUG